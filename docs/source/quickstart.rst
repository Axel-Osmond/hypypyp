Quickstart
==========

Import the two main packages:

.. code-block:: python

   import seth
   import hyp

The package ``seth`` contains finite named sets, named functions, and categorical constructions.

The package ``hyp`` contains hypergraphs represented as spans of finite named sets.