Terminology
===========

This project uses the following terminology for hypergraphs:

- ``node`` for node or vertex;
- ``link`` for edge or hyperedge;
- ``tie`` for incidence witness.

Mathematically, a hypergraph is represented as a span of sets.